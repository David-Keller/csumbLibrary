<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dak.csumblibrary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateAcc" />
        <activity android:name=".PlaceHoldLogin" />
        <activity android:name=".SystemAdminLogIn" />
        <activity android:name=".CancleHold" />
        <activity android:name=".HoldTimeManager" />
        <activity android:name=".AdminScreen" />
        <activity android:name=".PlaceHold" />
        <activity android:name=".CancleHoldList" />
        <activity android:name=".addBook"></activity>
    </application>

</manifest>